<%- model_class = UsuarioCliente -%>
<div class="page-header">
  <h1><%=t '.title', :default => model_class.model_name.human.pluralize.titleize %></h1>
</div>
<table class="table table-striped">
  <thead>
    <tr>
      <th><%= model_class.human_attribute_name(:id) %></th>
      <th><%= model_class.human_attribute_name(:nombre) %></th>
      <th><%= model_class.human_attribute_name(:apellidos) %></th>
      <th><%= model_class.human_attribute_name(:provincia) %></th>
      <th><%= model_class.human_attribute_name(:canton) %></th>
      <th><%= model_class.human_attribute_name(:direccion) %></th>
      <th><%= model_class.human_attribute_name(:telefono) %></th>
      <th><%= model_class.human_attribute_name(:email) %></th>
      <th><%= model_class.human_attribute_name(:created_at) %></th>
      <th><%=t '.actions', :default => t("helpers.actions") %></th>
    </tr>
  </thead>
  <tbody>
    <% @usuario_clientes.each do |usuario_cliente| %>
      <tr>
        <td><%= link_to usuario_cliente.id, usuario_cliente_path(usuario_cliente) %></td>
        <td><%= usuario_cliente.nombre %></td>
        <td><%= usuario_cliente.apellidos %></td>
        <td><%= usuario_cliente.provincia %></td>
        <td><%= usuario_cliente.canton %></td>
        <td><%= usuario_cliente.direccion %></td>
        <td><%= usuario_cliente.telefonos.first.telefono %></td>
        <td><%= usuario_cliente.emails.first.email %></td>
        <td><%=l usuario_cliente.created_at %></td>
        <td>
          <%= link_to t('.edit', :default => t("helpers.links.edit")),
                      edit_usuario_cliente_path(usuario_cliente), :class => 'btn btn-mini' %>
          <%= link_to t('.destroy', :default => t("helpers.links.destroy")),
                      usuario_cliente_path(usuario_cliente),
                      :method => :delete,
                      :data => { :confirm => t('.confirm', :default => t("helpers.links.confirm", :default => 'Are you sure?')) },
                      :class => 'btn btn-mini btn-danger' %>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>

<%= link_to t('.new', :default => t("helpers.links.new")),
            new_usuario_cliente_path,
            :class => 'btn btn-primary' %>
